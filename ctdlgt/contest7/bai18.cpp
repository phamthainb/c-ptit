/**
 * Trung tố
*/
#include <bits/stdc++.h>
using namespace std;

string s;
vector<string> number;

void ht()
{
    //convert to hau to
    for (int i = 0; i < s.size(); i++)
    {
        // if (s[i] =)
    }
}
int tt() {}
int main()
{
    int times;
    cin >> times;
    while (times--)
    {
        cin >> s;
        number.clear();
        ht(); //hau to
        cout << tt() << endl;
    }
}